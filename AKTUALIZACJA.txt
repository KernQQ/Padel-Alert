PADLETIC — DESKTOP CLEAN RESET 10

WAŻNE:
To jest desktop-only override. Nie zastępuj nim mobile CSS.
Ma być importowany JAKO OSTATNI CSS w frontend/src/App.jsx.

DODAJ NA KOŃCU IMPORTÓW CSS:
import "./styles/desktop-clean-reset.css";

Nie usuwaj:
- mobile-1to1.css
- mobile-platform-fix.css

Po wdrożeniu desktop:
- stare /premium/hero.jpg znika
- brak limonki/zielonego brandingu
- sidebar jest granatowo-grafitowy
- aktywne elementy są neutralne off-white / granat
- mobile <=900 px pozostaje bez zmian

STOPKA:
cd C:\PROJEKTY\Padel-Alert
Remove-Item -Recurse -Force frontend\node_modules\.vite -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force frontend\dist -ErrorAction SilentlyContinue
git add .
git commit -m "Padletic desktop clean reset 10"
git push
